package core.basesyntax;

public enum Colors {
    //black, // this is a constant field, so it is better to use UPPER_CASE
    // white,
    // red,
    // blue
    BLACK,
    WHITE,
    RED,
    BLUE
}
